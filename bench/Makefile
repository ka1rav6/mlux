# ============================================================================
#  mlux/bench/Makefile — benchmarks (placeholder)
# ============================================================================

.PHONY: all clean

all:
	@echo "  BENCH   no benchmarks yet — add bench files to bench/ and re-run."

clean:
	@rm -rf $(BUILD_OUT)/bench
