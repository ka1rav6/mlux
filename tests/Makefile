# ============================================================================
#  mlux/tests/Makefile — test suite (placeholder)
# ============================================================================

.PHONY: all clean

all:
	@echo "  TEST    no tests yet — add test files to tests/ and re-run."

clean:
	@rm -rf $(BUILD_OUT)/tests
